<form method="post" action="{{ path('conteiner_movimentacoes_delete', {'id': conteiner_movimentaco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conteiner_movimentaco.id) }}">
    <button class="btn">Delete</button>
</form>
